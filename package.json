"scripts": {
  "dev": "vite",
  "prebuild": "node -e \"console.log('PWD:', process.cwd()); const fs=require('fs'); console.log('FILES:'); console.log(require('child_process').execSync('ls -la').toString());\"",
  "build": "vite build",
  "preview": "vite preview"
}
