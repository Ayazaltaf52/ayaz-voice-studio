import React from 'react'
import ReactDOM from 'react-dom/client'
import VoiceGeneratorApp from './voice_generator'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VoiceGeneratorApp />
  </React.StrictMode>,
)
